@model ProjectBlog.ViewModels.Header

<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
        <a class="navbar-brand" href="/">@Html.Sitecore().Field("Site name")</a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                @foreach (var menuItem in Model.NavButtons)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@menuItem.Url">@menuItem.Name</a>
                    </li>
                }
                <li class="nav-item">
                    <a class="nav-link" href="@Model.LangSwitchUrl">@Model.LangButton</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<!-- Page Header -->
<header class="masthead" style="background-image: url(@Model.BackgroundUrl)">
    <div class="overlay"></div>
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-10 mx-auto">
                <div class="site-heading">
                    <h1>@Html.Sitecore().Field("Site heading")</h1>
                    <span class="subheading">@Html.Sitecore().Field("Sub heading")</span>
                </div>
            </div>
        </div>
    </div>
</header>